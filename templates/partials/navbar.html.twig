{# templates/partials/navbar.html.twig #}
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<div
		class="container-fluid">
		{# <a class="navbar-brand fw-bold" href="{{ path('homepage') }}">Caisse Restaurant</a> #}
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu" aria-controls="navbarMenu" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarMenu">
			<ul
				class="navbar-nav me-auto mb-2 mb-lg-0">
				{# Accès admin EasyAdmin #}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('admin') }}">Admin</a>
				</li>

				{# Accès aux tables #}
			{# <li class="nav-item">
											<a class="nav-link" href="{{ path('app_caisse_tables') }}">Tables</a>
										</li> #}

				{# Rapports / statistiques #}
				{# <li class="nav-item">
													<a class="nav-link" href="{{ path('app_caisse_stats') }}">Statistiques</a>
												</li> #}

				{# Paramètres / configuration #}
				{# <li class="nav-item">
													<a class="nav-link" href="{{ path('app_caisse_settings') }}">Paramètres</a>
												</li> #}
			</ul>

			{# Optionnel : connexion utilisateur / profil #}
			{# <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
										{% if app.user %}
											<li class="nav-item dropdown">
												<a class="nav-link dropdown-toggle" href="#" id="userMenu" role="button" data-bs-toggle="dropdown" aria-expanded="false">
													{{ app.user.username }}
												</a>
												<ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userMenu">
													<li>
														<a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a>
													</li>
												</ul>
											</li>
										{% else %}
											<li class="nav-item">
												<a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
											</li>
										{% endif %}
									</ul> #}
		</div>
	</div>
</nav>
